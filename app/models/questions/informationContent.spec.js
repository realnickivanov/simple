define(['models/questions/informationContent'], function (InformationContent) {
    "use strict";

    describe('model [InformationContent]', function () {
        it('should be function', function() {
            expect(InformationContent).toBeFunction();
        });
    });
});